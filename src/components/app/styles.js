import styled from 'styled-components';

const Main = styled.main`
    display: flex;
    flex-wrap: wrap;
    width: 1190px;
    margin: 0 auto;
    padding: 0;
    flex-direction: row;
    justify-content: space-between;
`;

export { Main };